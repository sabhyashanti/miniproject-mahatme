import React from 'react';

function TVSimulation() {
  return (
    <div style={{ padding: '20px', backgroundColor: '#000', color: '#fff', height: '100vh' }}>
      <h1>Digital Display Board (Kiosk Mode)</h1>
      <p>This is what the patients will see on the TV in the waiting room.</p>
    </div>
  );
}

export default TVSimulation;